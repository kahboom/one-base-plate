# Workflows (reusable how-tos)

Short, procedural docs that any agent can follow. **Not** for one-line global rules (those go in [`global-rules.md`](../global-rules.md)) or architectural rationale (that goes in [`decision-log.md`](../decision-log.md)).

| Workflow | Use when |
| -------- | -------- |
| [`prd-feature-slice.md`](./prd-feature-slice.md) | Implementing or verifying a scoped PRD feature |

Add new files here when a repeatable multi-step process is shared across agents.
